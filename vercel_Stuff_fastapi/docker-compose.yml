version: '3.8'

services:
  web:
    build: .
    container_name: ai-risk-prediction
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      PORT: 5000
    volumes:
      - .:/app
    command: python app.py
